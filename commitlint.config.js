const Configuration = {
  extends: ["@commitlint/config-conventional"],
  /**
     rules: {
        'type-enum': [
        2,
        'always', 
            ['feat', 'fix', 'refactor', 'revert', 'style', 'test', 'docs', 'chore']
        ]
     }
     */
};

export default Configuration;
